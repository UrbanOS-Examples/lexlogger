apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ template "logstack.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    component: "{{ .Values.elasticsearch.master.name }}"
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "logstack.fullname" . }}-discovery
spec:
  clusterIP: None
  ports:
    - port: 9300
      targetPort: transport
  selector:
    app: {{ template "logstack.name" . }}
    component: "{{ .Values.elasticsearch.master.name }}"
    release: {{ .Release.Name }}